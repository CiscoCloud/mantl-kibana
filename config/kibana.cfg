template {
  source = "/consul-template/template.d/kibana.yml.tmpl"
  destination = "/opt/kibana/config/kibana.yml"
  command = "bash -c '((pid=$(pidof /opt/kibana/bin/../node/bin/node) && kill $pid) || true) && cat /opt/kibana/config/kibana.yml && kibana' &"
}